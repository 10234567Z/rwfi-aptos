// Mock helpers for frontend development
// This bypasses the Node.js dependency issues

export const isValidNetworkName = (network: any) => {
  // For mock purposes, always return true
  return true;
};
